# Core config loading
CHAPTER_SPLITTER_CONFIG=
CHAPTER_SPLITTER_CONFIG_TIMEOUT_SECONDS=5.0

# start script mode: gui or split
CHAPTER_SPLITTER_MODE=gui

# Required only when CHAPTER_SPLITTER_MODE=split
PDF_PATH=
CHAPTERS_PATH=
